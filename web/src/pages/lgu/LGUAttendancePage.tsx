import { GlobalNav } from '../../components/ui/GlobalNav';
import { SubNavFrosted } from '../../components/ui/SubNavFrosted';

export default function LGUAttendancePage() {
  return (
  <div className="p-6">
    <h1 className="text-[34px] font-semibold">Attendance Monitor</h1>
    <p className="mt-4 text-ink-muted-80">Live volunteer check-ins appear here. Select an event in the mobile app to scan QR codes.</p>
  </div>
  );
}
